# 第43章 日志结构文件系统 LFS

## 1. 本章核心

日志结构文件系统，全称 **Log-structured File System，LFS**。

它的核心思想是：

> 不再原地覆盖旧数据，而是把所有更新都先缓存在内存中，攒成一个大的 segment，然后顺序写入磁盘空闲区域。

一句话总结：

> **LFS 把整个文件系统当成一条不断追加的日志来写，用大顺序写提升写入性能，再通过垃圾收集回收旧空间。**

------

## 2. LFS 的设计动机

LFS 出现的背景主要有四点。

### 2.1 内存越来越大，读请求更多命中缓存

随着内存变大，文件系统可以缓存更多数据。

因此：

```text
很多读请求可以直接从内存缓存中完成；
真正落到磁盘上的 I/O 中，写入占比越来越高。
```

所以文件系统性能越来越取决于 **写入性能**。

------

### 2.2 随机 I/O 和顺序 I/O 差距很大

磁盘 I/O 的主要开销包括：

```text
寻道时间 + 旋转延迟 + 数据传输时间
```

顺序写可以减少大量寻道和旋转延迟，因此远快于随机写。

随着磁盘带宽提升，顺序 I/O 和随机 I/O 的性能差距越来越明显。

------

### 2.3 传统文件系统小文件写入效率差

比如 FFS 创建一个只有一个数据块的小文件，可能需要写：

```text
1. 新 inode
2. inode 位图
3. 目录数据块
4. 目录 inode
5. 文件数据块
6. 数据位图
```

这些写入虽然可能在同一个块组内，但仍然会产生很多小范围随机写。

------

### 2.4 RAID 小写入问题

RAID-4 和 RAID-5 有 small-write problem。

一次逻辑小写入可能导致：

```text
4 次物理 I/O
```

传统文件系统通常没有很好地避免这种问题。

------

## 3. LFS 的基本思想

传统文件系统通常会原地更新数据：

```text
修改数据块 → 覆盖原位置
修改 inode → 覆盖原位置
修改 bitmap → 覆盖原位置
```

而 LFS 的做法是：

```text
所有更新都写到新的空闲位置；
旧版本暂时保留在磁盘上；
之后通过垃圾收集清理旧版本。
```

也就是说，LFS 是一种 **追加式写入** 文件系统。

------

## 4. 如何把写入变成顺序写？

LFS 的关键问题是：

> 文件系统如何把所有写入都变成顺序写？

读请求很难控制，因为要读的数据可能在磁盘任意位置。

但写入不一样：

```text
写到哪里，是文件系统可以决定的。
```

所以 LFS 选择：

```text
把新数据、新 inode、新 imap 等元数据都写到磁盘空闲区域。
```

这样就能把很多随机小写入转化成连续的大顺序写。

------

## 5. 写入缓冲与 segment

仅仅“写到连续地址”还不够。

如果先写地址 A，过一小会儿再写 A+1，虽然地址连续，但磁盘已经旋转过去了，第二次写仍然可能等待旋转。

所以高效写入需要：

```text
一次性写入大量连续数据
```

LFS 使用 **写入缓冲 write buffering**：

```text
先把更新缓存在内存里；
等攒够一大批；
再一次性顺序写入磁盘。
```

LFS 一次写入的大块区域叫：

```text
segment，段
```

这里的 segment 不是内存分段中的段，而是 LFS 中用于组织大顺序写的磁盘区域。

一个 segment 中可以包含：

```text
数据块
inode
inode map 片段
目录数据块
其他元数据
```

------

## 6. segment 应该多大？

设：

```text
Tposition = 每次写入前的定位时间
Rpeak = 磁盘峰值传输速率
D = 一次写入的数据量
```

写入时间：

```text
Twrite = Tposition + D / Rpeak
```

有效写入速率：

```text
Reffective = D / Twrite
```

即：

```text
Reffective = D / (Tposition + D / Rpeak)
```

如果希望有效速率达到峰值速率的 F 倍：

```text
Reffective = F × Rpeak
```

可以推出：

```text
D = F / (1 - F) × Rpeak × Tposition
```

### 例子

假设：

```text
Tposition = 10ms = 0.01s
Rpeak = 100MB/s
```

如果希望达到峰值带宽的 90%：

```text
F = 0.9

D = 0.9 / 0.1 × 100MB/s × 0.01s
D = 9MB
```

所以至少要一次写约 9MB，才能达到峰值带宽的 90%。

如果要求更高：

```text
95% 峰值带宽：约 19MB
99% 峰值带宽：约 99MB
```

结论：

> 越接近峰值带宽，需要缓冲的数据量越大。

------

## 7. LFS 的 inode 查找问题

传统 UNIX 文件系统中，inode 通常在磁盘固定区域。

因此可以通过：

```text
inode 地址 = inode 区起始地址 + inode 号 × inode 大小
```

直接找到 inode。

但是 LFS 不一样。

LFS 不原地覆盖 inode，而是每次更新 inode 都把新版本写到新位置。

所以：

```text
同一个 inode 的最新版本会不断移动。
```

于是产生问题：

> 给定 inode 号，如何找到最新版 inode？

------

## 8. inode map

LFS 使用 **inode map，简称 imap** 解决这个问题。

imap 的作用是：

```text
inode 号 → 最新 inode 的磁盘地址
```

例如：

```text
imap[k] = A1
```

表示：

```text
inode 号为 k 的 inode 最新版本在磁盘地址 A1。
```

访问数据的路径变成：

```text
inode 号
   ↓
imap
   ↓
最新 inode 的磁盘地址
   ↓
inode
   ↓
数据块地址
```

imap 本质上是一个间接层。

它让 inode 可以自由移动，而不需要修改所有引用它的地方。

------

## 9. imap 自己在哪里？

imap 也需要持久化到磁盘。

但如果把 imap 固定放在磁盘某处，每次更新 inode 都要跑去固定位置更新 imap，就会产生随机写。

这会破坏 LFS 的顺序写目标。

所以 LFS 的做法是：

```text
把 imap 的一部分也放进当前 segment 中，一起顺序写入。
```

例如更新文件 k 时，LFS 会顺序写入：

```text
1. 新数据块
2. 新 inode
3. 更新后的 imap 片段
```

------

## 10. 检查点区域 CR

新的问题来了：

> imap 片段也分散在磁盘各处，系统启动时怎么找到 imap？

文件系统必须有一个固定入口。

LFS 使用：

```text
Checkpoint Region，检查点区域，简称 CR
```

CR 位于磁盘上的固定位置，保存：

```text
最新 imap 片段的位置
日志头部和尾部信息
其他文件系统全局信息
```

启动时的查找路径：

```text
CR
 ↓
imap blocks
 ↓
inode
 ↓
data blocks
```

CR 不会每次写入都更新，而是周期性更新，比如每 30 秒左右更新一次。

这样可以避免频繁随机写。

------

## 11. LFS 读取文件的流程

假设内存中什么都没有，要读取一个文件。

流程如下：

```text
1. 读取检查点区域 CR
2. 通过 CR 找到 inode map 的位置
3. 读取 inode map，并缓存在内存中
4. 给定 inode 号，在 imap 中查找 inode 最新地址
5. 读取 inode
6. 根据 inode 中的直接指针、间接指针等找到数据块
7. 读取数据块
```

核心路径：

```text
CR → imap → inode → data
```

如果 imap 已经缓存在内存中，则读取过程和传统 UNIX 文件系统差不多，只是多了一次 imap 查找。

------

## 12. LFS 中目录如何存储？

LFS 中目录结构和传统 UNIX 文件系统基本相同。

目录本质上是：

```text
文件名 → inode 号
```

例如：

```text
foo → inode number k
```

注意目录保存的是 inode 号，不是 inode 的磁盘地址。

------

## 13. 创建文件时 LFS 写什么？

假设在目录 dir 中创建文件 foo。

LFS 需要写入：

```text
1. foo 的数据块
2. foo 的 inode
3. dir 的目录数据块
4. dir 的 inode
5. 更新后的 imap 片段
```

这些内容会被放入一个 segment 中顺序写入。

可以理解为：

```text
[foo 数据块][foo inode][dir 目录数据][dir inode][imap 片段]
```

------

## 14. 访问目录中的文件

访问文件 `/dir/foo` 时，大致流程是：

```text
1. 通过 imap 找到目录 dir 的 inode
2. 读取 dir 的 inode
3. 根据 dir inode 找到目录数据块
4. 读取目录数据块，找到 foo → inode 号 k
5. 通过 imap 找到 inode k 的最新位置
6. 读取 inode k
7. 根据 inode k 找到 foo 的数据块
```

路径可以表示为：

```text
imap → dir inode → dir data → foo 的 inode 号 → imap → foo inode → foo data
```

------

## 15. 递归更新问题

LFS 中 inode 的位置会不断变化。

如果目录中保存的是 inode 的磁盘地址，就会出现：

```text
文件 inode 位置变化
 ↓
目录数据需要更新
 ↓
目录 inode 位置变化
 ↓
父目录数据需要更新
 ↓
父目录 inode 位置变化
 ↓
继续向上更新……
```

这就是：

```text
recursive update problem，递归更新问题
```

LFS 通过 imap 避免这个问题。

因为目录保存的是：

```text
文件名 → inode 号
```

而不是：

```text
文件名 → inode 磁盘地址
```

所以 inode 位置变化时，只需要更新 imap，目录本身不用改变。

一句话：

> imap 作为间接层，避免了 inode 移动导致的递归更新。

------

## 16. LFS 的新问题：垃圾收集

LFS 不原地覆盖数据，而是不断写新版本。

因此磁盘上会残留很多旧版本：

```text
旧数据块
旧 inode
旧 imap
旧目录块
```

这些不再被当前文件系统引用的旧版本称为：

```text
garbage，垃圾
```

LFS 必须定期清理这些垃圾，否则磁盘空间会被耗尽。

------

## 17. 为什么按 segment 清理？

如果清理程序只是释放零散块，磁盘会变成：

```text
[used][free][used][free][used][free]
```

这样会产生很多小空洞。

后续写入就无法找到大块连续空间，也就无法保持高效顺序写。

因此 LFS 按 segment 清理。

基本过程：

```text
1. 读取 M 个旧 segment
2. 判断其中哪些块还活着
3. 把活块压缩写入 N 个新 segment
4. N < M
5. 释放旧的 M 个 segment
```

可以理解为：

```text
旧 segment：活块 + 死块混杂
        ↓
清理
        ↓
新 segment：只保存活块
        ↓
旧 segment 整体释放
```

------

## 18. 如何判断块是活的还是死的？

LFS 在每个 segment 的头部放置：

```text
Segment Summary Block，段摘要块，简称 SS
```

段摘要块记录每个块的信息：

```text
这个块属于哪个 inode？
这是该文件的第几个块？
```

也就是：

```text
inode 号 N
文件内偏移 T
```

------

### 判断流程

假设磁盘地址 A 上有一个数据块 D。

段摘要告诉我们：

```text
D 属于 inode N
D 是文件 N 的第 T 个块
```

判断过程：

```text
1. 从 Segment Summary 中得到 (N, T)
2. 通过 imap[N] 找到 inode N 的最新位置
3. 读取最新 inode N
4. 查看 inode N 的第 T 个块指针
5. 如果 inode[T] == A，说明块 D 是活的
6. 否则说明块 D 是垃圾
```

伪代码：

```c
(N, T) = SegmentSummary[A];
inode = Read(imap[N]);

if (inode[T] == A) {
    // block D is alive
} else {
    // block D is garbage
}
```

核心判断标准：

> 如果最新 inode 仍然指向这个块，它就是活的；否则就是垃圾。

------

## 19. 版本号优化

LFS 可以使用版本号来加速死活判断。

当文件被删除或截断时，LFS 增加文件版本号，并在 imap 中记录。

segment 中的块也记录版本号。

如果：

```text
segment 中记录的版本号 != imap 中当前版本号
```

说明该块属于旧版本，可以快速判断为垃圾。

这样可以避免额外读取 inode。

------

## 20. 清理策略：何时清理？

LFS 可以在以下时机清理：

```text
1. 周期性清理
2. 系统空闲时清理
3. 磁盘快满时清理
```

何时清理相对简单，难点在于清理哪些 segment。

------

## 21. 清理策略：清理哪些 segment？

LFS 原论文提出区分：

```text
hot segment
cold segment
```

### 21.1 热段 hot segment

热段中的数据经常被修改。

特点：

```text
里面的数据很可能很快又被覆盖。
```

策略：

```text
延迟清理热段。
```

因为再等等，热段中可能会出现更多垃圾，清理收益更高。

------

### 21.2 冷段 cold segment

冷段中的数据相对稳定。

特点：

```text
里面剩余的活块可能长期不变。
```

策略：

```text
尽早清理冷段。
```

这样可以尽快回收其中的死块，并把稳定的活块搬走。

------

## 22. 崩溃恢复问题

LFS 也要考虑崩溃恢复。

崩溃可能发生在：

```text
1. 正在写 segment 时
2. 正在更新 CR 时
```

------

## 23. CR 的原子更新

CR 是 LFS 的启动入口，必须保证一致性。

LFS 使用两个 CR：

```text
一个位于磁盘开头；
一个位于磁盘末尾；
二者交替更新。
```

更新 CR 时，使用时间戳协议：

```text
1. 写 CR 头部，带时间戳
2. 写 CR 主体
3. 写 CR 尾部，带时间戳
```

如果崩溃发生在 CR 更新中间，头尾时间戳可能不一致。

恢复时选择：

```text
时间戳一致，并且最新的 CR
```

这样可以避免使用写坏的 CR。

------

## 24. Roll Forward 前滚恢复

CR 不是每次写入都更新，而是周期性更新。

如果只恢复到最近一次 CR，那么最近一段时间的数据可能丢失。

因此 LFS 使用：

```text
roll forward，前滚
```

前滚的思想是：

```text
从最后一个一致的 CR 出发；
继续扫描后面的 segment；
检查其中是否存在有效更新；
如果有，就把这些更新恢复进文件系统状态。
```

恢复流程：

```text
1. 读取最新一致 CR
2. 恢复到检查点时的状态
3. 从 CR 记录的日志位置继续向后扫描
4. 检查后续 segment 中的有效更新
5. 更新 imap 和相关元数据
6. 尽量恢复检查点之后的写入
```

------

## 25. LFS 与 Journaling 的区别

LFS 和 journaling 都有“日志”这个词，但目标不同。

| 对比项       | Journaling 日志        | LFS 日志结构文件系统 |
| ------------ | ---------------------- | -------------------- |
| 主要目标     | 崩溃一致性             | 写入性能             |
| 写入方式     | 先写日志，再写真实位置 | 所有内容都追加写     |
| 是否原地覆盖 | 最终通常会覆盖原位置   | 通常不原地覆盖       |
| 日志的作用   | 用于恢复               | 磁盘布局本身         |
| 重点         | correctness            | performance          |

一句话区分：

```text
journaling 是“用日志保证安全”；
LFS 是“把整个文件系统写成日志”。
```

------

## 26. LFS 与 Copy-on-Write

LFS 的写入方式也可以看作：

```text
copy-on-write，写时复制
```

更新数据时，不修改旧位置，而是写到新位置。

旧版本之后可以：

```text
1. 被垃圾收集清理
2. 或者用于快照和版本恢复
```

类似思想也影响了许多现代文件系统，例如：

```text
WAFL
ZFS
btrfs
```

------

## 27. LFS 的优点

### 27.1 写入性能高

LFS 把很多随机小写入合并成大顺序写。

适合：

```text
小文件创建
频繁元数据更新
写密集型负载
```

------

### 27.2 能充分利用磁盘顺序带宽

传统文件系统容易产生随机写。

LFS 尽量把写入变成：

```text
连续追加写
```

因此可以接近磁盘峰值带宽。

------

### 27.3 imap 设计巧妙

imap 同时解决：

```text
1. inode 位置不断变化的问题
2. 递归更新问题
```

------

### 27.4 支持写时复制思想

LFS 的思想影响了后来的现代文件系统。

------

## 28. LFS 的缺点

### 28.1 会产生大量垃圾

因为每次更新都写新版本，旧版本会留在磁盘上。

------

### 28.2 需要垃圾收集

垃圾收集会带来额外开销。

尤其当磁盘空间紧张、活数据很多时，清理成本会很高。

------

### 28.3 读取不一定更快

LFS 主要优化写入。

读取时，如果数据分散，仍可能产生随机读。

不过 imap 缓存在内存中后，读取文件的 I/O 数量和传统 UNIX 文件系统差不多。

------

## 29. 本章整体结构图

### 29.1 查找文件

```text
CR
 ↓
imap
 ↓
inode
 ↓
data block
```

------

### 29.2 写入文件

```text
数据块 + inode + imap 片段 + 其他元数据
        ↓
      segment
        ↓
  顺序写入磁盘空闲区域
```

------

### 29.3 垃圾收集

```text
旧 segment
   ↓
判断活块和死块
   ↓
把活块写到新 segment
   ↓
释放旧 segment
```

------

### 29.4 崩溃恢复

```text
读取最新一致 CR
   ↓
恢复检查点状态
   ↓
roll forward 扫描后续 segment
   ↓
恢复有效更新
```

------

## 30. 易考点总结

### 30.1 LFS 的核心目标是什么？

提高写入性能，充分利用磁盘顺序带宽。

------

### 30.2 LFS 为什么不原地覆盖？

为了把所有更新都追加写到磁盘空闲区域，从而形成大顺序写。

------

### 30.3 segment 是什么？

LFS 一次写入磁盘的大块连续区域。

其中可以包含：

```text
数据块
inode
imap 片段
目录块
其他元数据
```

------

### 30.4 imap 解决什么问题？

imap 解决 inode 位置不断变化的问题。

```text
inode 号 → 最新 inode 磁盘地址
```

------

### 30.5 CR 是什么？

CR 是检查点区域。

它位于磁盘固定位置，保存最新 imap 片段的位置，是 LFS 启动和恢复的入口。

------

### 30.6 为什么需要 CR？

因为 imap 自己也分散在磁盘各处。

如果没有 CR，系统启动后不知道从哪里找到 imap。

------

### 30.7 LFS 如何读取文件？

```text
CR → imap → inode → data
```

如果 imap 已经缓存：

```text
imap → inode → data
```

------

### 30.8 目录在 LFS 中如何存储？

和传统 UNIX 文件系统类似：

```text
文件名 → inode 号
```

目录不直接保存 inode 的磁盘地址。

------

### 30.9 imap 如何避免递归更新？

inode 位置变化时，只更新 imap。

目录仍然保存 inode 号，不需要修改。

------

### 30.10 为什么 LFS 需要垃圾收集？

因为 LFS 不原地覆盖，旧版本的数据和元数据会残留在磁盘上。

这些旧版本需要被回收。

------

### 30.11 LFS 如何判断块是否活着？

通过 segment summary 得到：

```text
inode 号 N
文件内偏移 T
```

然后检查最新 inode：

```text
如果 inode[T] == 当前块地址 A
说明块活着；

否则说明块是垃圾。
```

------

### 30.12 清理策略中热段和冷段如何处理？

```text
热段：经常被修改，延迟清理
冷段：相对稳定，尽早清理
```

------

### 30.13 LFS 如何保证 CR 更新安全？

使用两个 CR，交替写入。

写 CR 时使用头尾时间戳。

恢复时选择时间戳一致且最新的 CR。

------

### 30.14 roll forward 是什么？

从最后一个检查点开始，扫描之后的日志段，尽量恢复检查点之后的有效更新。

------

## 31. 最终总结

LFS 的核心是：

```text
写入时：
    所有更新都组成 segment，顺序追加写入磁盘。

查找时：
    通过 CR 找 imap，通过 imap 找 inode，通过 inode 找数据块。

清理时：
    通过 segment summary 判断块死活，把活块搬到新 segment，释放旧 segment。

恢复时：
    通过两个 CR 和时间戳保证检查点一致，再用 roll forward 恢复最近更新。
```

最适合记忆的一句话：

> **LFS 写得爽，清理烦；用 segment 提速，用 imap 定位，用 CR 启动，用 GC 回收，用 roll forward 恢复。**